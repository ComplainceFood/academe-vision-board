import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.academe.visionboard',
  appName: 'Academe Vision Board',
  webDir: 'dist'
};

export default config;
